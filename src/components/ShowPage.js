import React from 'react'

export default function Show({ name, ended, genres, id, image, language,
    externals, rating, status, summary, url, previousepisode, ...props }) {
    return (
        <div>

        </div>
    )
}
